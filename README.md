# arvis-integration

Mock data for [Arvis](https://github.com/jopemachine/arvis) integration test.

## Test list

The intended tests for each command are as follows.

### Basic trigger tests

#### a1

* `keyword`

* `keyword` overlay (3 depth)

* `argType` test

#### a2

* `scriptFilter`

* `scriptFilter` overlay (3 depth)

* `argType` test

* `withspace` test

* (Optional) `popWork` test

* (Optional) `setModifierItem` test

#### a3

* `keyword` + `scriptFilter`

* `keyword` + `scriptFilter` overlay (2 depth)

#### a4

* `hotkey` + `action`

* `hotkey` + `keyword`

#### a5

* `hotkey` + `action`

* `hotkey` + `scriptfilter`

#### a6

* `scriptfilter` error test

#### a7

